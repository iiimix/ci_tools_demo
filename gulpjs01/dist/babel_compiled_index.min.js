"use strict";

function add(a, b) {
  console.log(a + b);
  return a + b;
}

;
var person = {
  name: 'iiimix',
  rich: '8900000'
};
var name = person.name,
    rich = person.rich;

var fn = function fn(p, s) {
  console.log(p, s);
};

fn('myName', "name: ".concat(name, ", rich: ").concat(rich));